.. include:: refs.rst

============
Installation
============

The `SLM` is both a Django_ app and a fully deployable website.

Development
###########

1. Install Poetry_.

2. Install a Relational Database Management System (RDBMS). `SLM` supports both
PostgreSQL, MySQL or MariaDB.

3. Clone the `SLM` from GitHub_::

       git clone

